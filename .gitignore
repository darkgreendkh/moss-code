# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd

# Virtual environments
.venv/
venv/
uv.lock

# Packaging
*.egg-info/
build/
dist/

# Test and lint caches
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Temporary files
*.tmp
*.temp
*.swp
*.swo
*~

# Local scratch space
tmp/
.worktrees/
.moss/
.mini-moss/
artifacts/
release/
# 评测污染真实 checkout 时留在**仓库根目录**的 fixture 副本。
# 必须锚定到根：不锚的话它会顺手吞掉 benchmarks/cassettes/*/readme_intro_locked/。
/readme_intro_locked/

# Local secrets and provider configuration
.env
.env.*
!.env.example

# 文档默认不发布，按目录显式放行。
# `docs/*` 会在目录这一层就把整棵子树挡住（git 不会下降进被忽略的目录），
# 所以每一层都要各写一条 `!`：先放行目录，再放行目录里的文件。
docs/*
!docs/README.md
!docs/architecture.md
!docs/features/
!docs/features/*.md
!docs/reference/
!docs/reference/*.md
!docs/decisions/
!docs/decisions/*.md
!docs/plans/
!docs/plans/active/
!docs/plans/active/*.md
!docs/plans/archive/
!docs/plans/archive/*.md
!docs/specs/
!docs/specs/*.md
# 个人材料，永不发布。放在放行规则之后，最后一条匹配的规则生效。
docs/面试话术/

# OS files
.DS_Store

# Packaging metadata
moss.egg-info/
